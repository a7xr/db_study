" so where there is pointer, there is a line
set cul

" so there_s line number
set nu

" to change the tabulation to 4 spaces
set expandtab ts=4 sw=4 ai
%retab

" to ignore case when u od a research
set ignorecase

" when u enter command like <c-w>, it_s going to be displaied in the right
set showcmd

" to highlight the search
set hlsearch

" to switch the current line with the below
map <c-k>j ddjP

" to switch the current line with the above
map <c-k>k ddkP

" so every lines will be limited to 80char
" set textwidth=80

